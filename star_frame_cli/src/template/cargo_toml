[package]
name = "{name_lowercase}"
description = "Built with Star Frame"
version = "0.0.1"
edition = "2021"
publish = false

[package.metadata]
release.release = false

[lib]
# Include `lib` for local tests and dev tooling while still supporting on-chain builds.
crate-type = ["cdylib", "lib"]
name = "{name_lowercase_underscore}"

[features]
no_entrypoint = []
idl = ["star_frame/idl"]

[profile.release]
overflow-checks = true
lto = "thin"
codegen-units = 1

[dependencies]
# TEMP: `cargo build-sbf` currently uses a Cargo version that cannot parse
# `edition2024` crates (e.g. `az` 1.3.0), so pin to 1.2.1 for compatibility.
# TODO(star_frame): remove this pin once build-sbf toolchains support Cargo
# that can parse `edition2024` dependencies.
az = "=1.2.1"
borsh = { version = "1.5.7", features = ["derive"] }
bytemuck = { version = "^1.22.0", features = ["extern_crate_std", "min_const_generics", "derive"] }
star_frame = { version = "0.28.0" }

[dev-dependencies]
mollusk-svm = "=0.7.0"
solana-account = "3.0.0"
